<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iikun.userservice.mapper.UserCollectionMapper">

    <insert id="insert">
        INSERT INTO user_collection (user_id, target_type, target_id)
        VALUES (#{userId}, #{targetType}, #{targetId})
    </insert>

    <delete id="delete">
        DELETE
        FROM user_collection
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </delete>

    <select id="exists" resultType="int">
        SELECT COUNT(1)
        FROM user_collection
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(1)
        FROM user_collection
        WHERE user_id = #{userId}
    </select>

    <select id="selectPageByUserId" resultType="com.iikun.userservice.entity.UserCollection">
        SELECT id,
               user_id     AS userId,
               target_type AS targetType,
               target_id   AS targetId,
               create_time AS createTime
        FROM user_collection
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

</mapper>